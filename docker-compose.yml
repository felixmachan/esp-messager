services:
  msgapi:
    build: .
    ports:
      - "80:8000"
    environment:
      - API_TOKEN=${API_TOKEN}
      - DB_PATH=/data/messages.db
    volumes:
      - msgdata:/data
    restart: unless-stopped

volumes:
  msgdata:
